---
phase: 05-code-reorganization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - seed/Screens/OnboardingView.swift
  - seed/Screens/TextInputView.swift
  - seed/Screens/RSVPDisplayView.swift
  - seed/Screens/HistoryView.swift
  - seed/Screens/SettingsView.swift
  - seed/Components/InputSourcePicker.swift
  - seed/Components/PlaybackControlsView.swift
  - seed/Components/CompletionStatsView.swift
  - seed/Components/ComprehensionQuizView.swift
  - seed/Components/DocumentPickerView.swift
  - seed/ContentView.swift
autonomous: true

must_haves:
  truths:
    - "App builds without errors after reorganization"
    - "App runs and maintains all v1 functionality"
    - "All screens accessible from their new locations"
  artifacts:
    - path: "seed/Screens"
      provides: "Screen-level views folder"
      min_files: 5
    - path: "seed/Components"
      provides: "Reusable components folder"
      min_files: 5
  key_links:
    - from: "ContentView.swift"
      to: "Screens/"
      via: "import statements"
      pattern: "import.*Screen"
---

<objective>
Reorganize v1 codebase into Screens/ and Components/ structure for v2 work.

Purpose: Clean separation before adding SwiftData library features.
Output: Restructured code with clear screen vs component distinction.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@seed/Presentation/Views/OnboardingView.swift
@seed/Presentation/Views/TextInputView.swift
@seed/Presentation/Views/RSVPDisplayView.swift
@seed/Presentation/Views/HistoryView.swift
@seed/Presentation/Views/SettingsView.swift
@seed/ContentView.swift
</context>

<tasks>

<task type="auto">
  <name>Create folder structure and move screen files</name>
  <files>
    seed/Screens/OnboardingView.swift,
    seed/Screens/TextInputView.swift,
    seed/Screens/RSVPDisplayView.swift,
    seed/Screens/HistoryView.swift,
    seed/Screens/SettingsView.swift
  </files>
  <action>
Create seed/Screens/ and seed/Components/ directories.

Move full-screen views to Screens/:
- OnboardingView.swift (first-run experience)
- TextInputView.swift (main input tab)
- RSVPDisplayView.swift (playback screen)
- HistoryView.swift (history tab)
- SettingsView.swift (settings tab)

Use `git mv` for clean history.
  </action>
  <verify>ls seed/Screens/ shows 5 files</verify>
  <done>Screen files moved to Screens/</done>
</task>

<task type="auto">
  <name>Move reusable component files</name>
  <files>
    seed/Components/InputSourcePicker.swift,
    seed/Components/PlaybackControlsView.swift,
    seed/Components/CompletionStatsView.swift,
    seed/Components/ComprehensionQuizView.swift,
    seed/Components/DocumentPickerView.swift
  </files>
  <action>
Move reusable components to Components/:
- InputSourcePicker.swift (paste/url/pdf selector)
- PlaybackControlsView.swift (play/pause controls)
- CompletionStatsView.swift (stats display)
- ComprehensionQuizView.swift (quiz UI)
- DocumentPickerView.swift (PDF picker wrapper)

Use `git mv` for clean history.
  </action>
  <verify>ls seed/Components/ shows 5 files</verify>
  <done>Component files moved to Components/</done>
</task>

<task type="auto">
  <name>Update imports and build verification</name>
  <files>seed/ContentView.swift</files>
  <action>
Update ContentView.swift imports if needed (Xcode may auto-fix).

Build project: xcodebuild -project seed.xcodeproj -scheme seed build

Fix any import errors that arise.
  </action>
  <verify>xcodebuild succeeds with "BUILD SUCCEEDED"</verify>
  <done>Project builds after reorganization</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] seed/Screens/ exists with 5 screen files
- [ ] seed/Components/ exists with 5 component files
- [ ] seed/Presentation/Views/ empty or removed
- [ ] xcodebuild succeeds
- [ ] App runs in simulator with all v1 features working
</verification>

<success_criteria>
- All tasks completed
- Project builds without errors
- All v1 functionality maintained
- CODE-01, CODE-02, CODE-03 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-reorganization/05-01-SUMMARY.md`
</output>
